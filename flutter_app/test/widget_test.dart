// This file intentionally left as a placeholder.
// The app uses a web-only architecture (dart:html) which requires
// browser-based tests. See test/widgets/ for widget tests run with
// --platform chrome, and test/models/ + test/services/ for VM unit tests.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder – see test/models/ and test/widgets/ for actual tests', () {
    expect(true, isTrue);
  });
}
